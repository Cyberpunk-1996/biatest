pipeline {
    agent any
    stages {
        stage('Setup') {
            steps {
                sh "pipx install -r requirements.txt"
            }
        }
        stage('Test') {
            steps {
                sh "pytest"
                
            }
        }    
            
    }
}